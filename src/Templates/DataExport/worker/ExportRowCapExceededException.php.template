<?php

declare(strict_types=1);

namespace App\Lib\DataExport;

/**
 * Generated by `php stone generate data-export`. Thrown by CursorStreamer
 * when a table's row count for one export job exceeds config('row_cap') —
 * caught by ExportWorker's job loop and turned into a fail_export_job()
 * call with a clear, actionable error_detail, never a silent truncation of
 * the exported data.
 */
final class ExportRowCapExceededException extends \RuntimeException
{
}
